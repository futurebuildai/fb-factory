# FB Ops

Read-only platform operations micro app for FutureBuild Cloud: FB Console
health probes and runtime facts as `defineAction()` calls, callable from the
dashboard UI and the shared agent chat. Part of the FB Factory workspace.

Run locally from the repo root:

```
pnpm --filter fb-ops dev
```

See the workspace DEVELOPMENT.md for environment and database setup.
